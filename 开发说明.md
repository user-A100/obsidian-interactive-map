# Interactive Map 插件 · 开发说明 / 交接文档

> 给接手的 AI 或人：本文档是**唯一权威信息源**，记录了插件机制、生成配方、各地图当前状态和踩过的坑。
> 读完即可继续维护。最后更新：2026-07-13。

---

## 1. 这是什么

一个 Obsidian 插件（纯 JS，无需编译，`main.js` 直接运行），在笔记里用代码块嵌入 SVG 地图：
- 鼠标悬浮：区域放大变色
- 点击区域：跳转到对应笔记（不存在则自动新建）
- 笔记里再嵌一张下级地图 → 实现 **省 → 市 → 区/县 层层下钻**；没有下级地图就是普通 md 笔记
- 悬浮 + Ctrl：弹出该笔记的**原生页面预览**（和 wikilink 一样）

## 2. 文件布局

```
.obsidian/plugins/interactive-map/
  manifest.json        插件清单
  main.js              插件代码（改了必须「重载插件」）
  styles.css           悬浮样式（CSS 变量 --im-hover-scale / --im-hover-color）
  data.json            设置：createIfMissing/defaultBaseFolder/hoverScale/hoverColor/openInNewTab
  README.md            用户向说明
  开发说明.md          ← 本文档
  scripts/
    build_map.js       通用：GeoJSON → 省级/市级可下钻 SVG + .json
    build_china.js     中国地图专用（含南海诸岛左下角小图框）

归档/attachment/                          ← 所有地图资产在这
  china_provinces_map.svg + .json         中国地图（34 省）
  上海市.svg/.json、安徽省、河北省、重庆市  省级图（→ 市/区）
  广东省.svg/.json、广东省.geojson         广东市级图（21 市）+ 源数据
  china_beauty.svg                        ⚠️ 精细 Illustrator 底图（1.7MB）——不可用，见第 7 节

Project/MAP/                              ← 区域笔记目录（data.json 的 defaultBaseFolder）
  中国地图.md、上海.md、安徽.md、河北.md、重庆.md、内蒙古.md、广东.md  各嵌一张地图
```

## 3. 地图三件套（缺一不可）

每个可下钻地图由**同名**的 `.svg` + `.json` 组成，放在同目录；外加一个 `.md` 笔记内嵌它。

**笔记**（如 `Project/MAP/广东.md`）：
~~~
```interactive-map
[[广东省.svg]]
```
~~~

**SVG 约定**——每个区域是一个带 `class="state <slug>"` 的 `<path>`，并**内嵌** `<title>`：
```xml
<path class="state 440100" fill="#BFBFBF" fill-rule="evenodd" stroke="#fff" stroke-width="0.02" d="M..Z"><title>广州市</title></path>
```
**JSON 侧车**（`广东省.json`）——把 slug 映射成中文（tooltip + 跳转目标笔记名）：
```json
{ "regions": { "440100": "广州市", "440300": "深圳市", ... } }
```

### 插件如何解析（`main.js` 的 `renderMap`）
1. 代码块里 `[[xxx.svg]]` → 解析为 TFile
2. `DOMParser` 解析 SVG → `importNode` 进 DOM
3. `querySelectorAll("[class*='state']")` 找区域 → 每个 set `data-slug`/`data-name`/`title`
4. 解析同名 `.json`（`loadSidecar`）→ `regions[slug]` 决定中文名（侧车优先 > 内置34省拼音表 > slug 本身）
5. 点击 → `openRegion(name)` → 在 `defaultBaseFolder` 下找/建笔记
6. 悬浮 + Ctrl → `trigger('hover-link')` → 核心 page-preview 弹层

**slug 用 adcode（6 位）最稳**：唯一、不依赖拼音转写。

## 4. 生成配方（标准流程）

数据源：阿里 DataV `https://geo.datav.aliyun.com/areas_v3/bound/<adcode>_full.json`
- 中国 = `100000`，省用省级 adcode（见下表），`_full` 直接给下一级（省→地级市；直辖市/重庆→区县）

**省级/市级图**（一行命令）：
```bash
# 把下载的 geojson 放进 归档/attachment/，然后：
node ".obsidian/plugins/interactive-map/scripts/build_map.js" "归档/attachment/广东省.geojson" 广东省
# → 生成 广东省.svg + 广东省.json（自动覆盖/创建）
```
再建/确保 `Project/MAP/广东.md` 内嵌 `[[广东省.svg]]`。完事。

**中国地图**（含南海诸岛小图框，一般不用动）：
```bash
# 先把 100000_full.json 存成 归档/attachment/china.geojson
node ".obsidian/plugins/interactive-map/scripts/build_china.js"
```

常用 adcode：北京110000 上海310000 河北130000 山西140000 内蒙古150000
辽宁210000 吉林220000 黑龙江230000 江苏320000 浙江330000 安徽340000
福建350000 江西360000 山东370000 河南410000 湖北420000 湖南430000
广东440000 广西450000 海南460000 重庆500000 四川510000 贵州520000
云南530000 西藏540000 陕西610000 甘肃620000 青海630000 宁夏640000
新疆650000 台湾710000 香港810000 澳门820000

## 5. 各地图当前状态（已验证可用）

| 地图 | 文件 | 内容 | slug |
|------|------|------|------|
| 中国 | china_provinces_map.svg/.json | 34 省 + 左下角南海诸岛小图框 | adcode，短省名 |
| 上海市 | 上海市.svg/.json | 16 区 | adcode |
| 安徽省 | 安徽省.svg/.json | 16 地级市 | adcode |
| 河北省 | 河北省.svg/.json | 11 地级市 | adcode |
| 重庆市 | 重庆市.svg/.json | 38 区/县 | adcode |
| 广东省 | 广东省.svg/.json | 21 地级市 | adcode |

下钻链路示例：`中国地图.md → china_provinces_map.svg → 点「广东」→ 广东.md → 广东省.svg → 点「广州市」→ 广州市.md`（不存在则自动新建）。

## 6. ⚠️ 已踩过的坑（务必避开）

1. **精细 Illustrator 底图（`china_beauty.svg`，1.7MB）不可用**。原图、以及用 Douglas-Peucker 简化到 287KB 的版本，在 Obsidian 里都"无法使用"（点不动/不显示）。**根因未完全定位**（不只是体积——简化版结构同 DataV 仍失败）。结论：中国地图只能用 DataV 版，别再尝试 china_beauty。备份在 `$CLAUDE_JOB_DIR/tmp/backup_orig/`（job 结束会清，需要可从 DataV 重生成）。

2. **绝不要在 `<svg>` 根下放 `<title>`**。SVG 的 tooltip 取"最近祖先 `<title>`"，根 title 会让所有区域显示同一个名（曾导致中国地图所有省都显示"中国"）。每个区域**自己内嵌** `<title>`。（`build_map.js` 已这样做。）

3. **省级图必须清洗头部**：DataV/Illustrator 导出的 SVG 常带 `<?xml?>`、`<?xml-stylesheet?>`、`<!DOCTYPE>`，会干扰插件解析。生成脚本输出的是干净 `<svg xmlns viewBox preserveAspectRatio>`，没问题。

4. **改了 SVG/.json/.md → 重渲染笔记**（重开 / 源码↔阅读视图切一次）；**改了 main.js → 必须重载插件**（设置→第三方插件→关再开，或重启 Obsidian）。插件按 mtime 缓存 SVG。

5. **南海诸岛**（海南的离岛，纬度低到 3.8°）会把 bbox 拉扁主体大陆。中国地图用左下角小图框分离（`build_china.js` 的 THRESH=18°）。省级图一般无此问题。

6. **短名 vs 长名**：DataV 给"广西壮族自治区/香港特别行政区/北京市"。侧车要用**短名**（广西/香港/北京）才能匹配既有笔记。`build_china.js` 已做剥离；`build_map.js` 用原名（市/区名通常带"市/区"后缀，与 DataV 一致，没问题）。

7. **陕西的 slug 是 `shanxiHZ`**（山西是 `shanxi`）——用 adcode 当 slug 可避开此坑；用拼音才需注意。

## 7. Ctrl 悬浮预览（`main.js`）

- `setupHoverPreview()`：在 onload 注册 document `keydown`(Ctrl/Meta) 和 `keyup` 监听，跟踪 `this.imHover`（当前鼠标所在区域 + 坐标）。按 Ctrl 时合成 `MouseEvent`(ctrlKey=true) 重新 `trigger('hover-link')` → 这样"先悬浮、后按 Ctrl（不移动鼠标）"也能弹。松开 Ctrl 时清 `lastKey`，确保重复按 Ctrl 也能触发。
- `imPreview(ev,target,hoverParent,sourcePath)`：去重后 `trigger('hover-link')`，核心 page-preview 接管。
- `renderMap` 里 **mouseover 仅记录坐标和 target，不再无条件触发 hover-link**；只有 `ev.ctrlKey || ev.metaKey` 时才调 `imPreview()`。mousemove 同理只有 Ctrl 按下时触发。**原因**：无 Ctrl 的 mouseover 先 trigger 会占用 `hoverParent`，导致后续 Ctrl 按下时 Obsidian 忽略第二次 trigger——已有笔记的省份因第一次 trigger 已创建 popover 状态而失效。
- 依赖核心插件「页面预览(Page Preview)」开启（默认开）。用户设置里 Ctrl 模式 vs 悬浮模式由 Obsidian 决定。

## 8. 如何新增一级下钻（如 市→区）

1. 从 DataV 下载该市的 `<市adcode>_full.json`（如广州=440100），存到 `归档/attachment/<市名>.geojson`
2. `node scripts/build_map.js "归档/attachment/<市名>.geojson" <市名>`（如 `广州市`）
3. 建 `Project/MAP/<市名>.md`（如 `广州市.md`），内嵌 `[[<市名>.svg]]`
4. 重渲染即可。点该市 → 进市笔记 → 看到区地图 → 点区 → 进区笔记。

## 9. 备份与回退

- 原始精细底图、DataV 中国地图各版本备份在 job 的 `tmp/backup_orig/`（job 结束自动清理）。
- 中国地图若出问题：`node scripts/build_china.js` 一键重生成可用 DataV 版。
- 省级图任何时候都能从 geojson 用 `build_map.js` 重生成。
